import { moduleMetadata } from '@storybook/angular';
import { Meta } from '@storybook/angular/types-6-0';
import { <%= props.camelCase %>Module } from './<%= props.componentName %>-component.module';
import { <%= props.camelCase %>Component } from './<%= props.componentName %>.component';

export default {
  title: 'components/<%= props.componentName %>',
  decorators: [
    moduleMetadata({
      imports: [
        <%= props.camelCase %>Module
      ]
    })
  ],
  parameters: {
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    }
  }
} as Meta;

// eslint-disable-next-line @typescript-eslint/naming-convention
export const Default = (args: <%= props.camelCase %>Component) => ({
  component: <%= props.camelCase %>Component,
  props: args
});

Default.args = {};
