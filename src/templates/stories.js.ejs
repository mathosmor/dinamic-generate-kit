import { moduleMetadata } from '@storybook/angular';
import { Meta } from '@storybook/angular/types-6-0';

import { <%= props.camelCase %>Module } from './<%= props.componentName %>-<%= props.typeLow %>.module';
import { <%= props.camelCase %><%= props.type %> } from './<%= props.componentName %>.<%= props.typeLow %>';

export default {
  title: '<%= props.namePath %>/<%= props.componentName %>',
  decorators: [
    moduleMetadata({
      imports: [
        <%= props.camelCase %>Module
      ]
    })
  ],
  parameters: {
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    }
  }
} as Meta;

// eslint-disable-next-line @typescript-eslint/naming-convention
export const Default = (args: <%= props.camelCase %><%= props.type %>) => ({
  component: <%= props.camelCase %><%= props.type %>,
  props: args
});

Default.args = {};
