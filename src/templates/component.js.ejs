import { Component, OnInit } from '@angular/core';

@Component({
  selector: '<%= props.selector %>-<%= props.componentName %>',
  templateUrl: './<%= props.componentName %>.<%= props.typeLow %>.html',
  styleUrls: ['./<%= props.componentName %>.<%= props.typeLow %>.scss']
})
export class <%= props.camelCase %><%= props.type %> implements OnInit {

  constructor() { }

  ngOnInit(): void {

  }

}
